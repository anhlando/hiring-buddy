<span class="highlight">
  <slot />
</span>

<style is:global>
  .highlight {
    @apply relative z-[1];
  }

  .highlight::before {
    content: "";
    position: absolute;
    bottom: 10%;
    left: -0.5rem;
    width: calc(100% + 1rem);
    height: 70%;
    @apply bg-lightningYellow;
    z-index: -1;
    transform: translateZ(-1px);
  }
</style>
