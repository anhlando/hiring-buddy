---
import logoFull from "../assets/logoFull.svg";

const links = [
  {
    href: "/our-story",
    label: "Our Story",
  },
  {
    href: "/our-team",
    label: "Team",
  },
  {
    href: "/client",
    label: "Client",
  },
];
---

<header class="bg-white shadow-md">
  <div class="container mx-auto">
    <div class="flex items-center justify-between">
      <div class="flex">
        <div class="mr-20 p-3 max-w-xxs">
          <a href="/">
            <img
              src={logoFull.src}
              width={logoFull.width}
              height={logoFull.height}
              alt="Hiring Buddy."
            />
          </a>
        </div>

        <nav class="inline-flex">
          {
            links.map((link) => (
              <a
                class="inline-flex justify-center items-center mr-6 text-gray-500"
                href={link.href}
              >
                {link.label}
              </a>
            ))
          }
        </nav>
      </div>

      <button>Join wait list!</button>
    </div>
  </div>
</header>
